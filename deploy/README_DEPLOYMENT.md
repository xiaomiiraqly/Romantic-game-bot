# 🚀 Развертывание Telegram бота для игры пар

## 📋 Описание
Telegram бот для интерактивной игры пар, который помогает создать романтическую атмосферу и углубить отношения через откровенные разговоры и задания.

## 🎯 Возможности бота
- **Игра для 4 человек (2 пары)** с пошаговыми заданиями
- **4 уровня сложности**: Знакомство → Флирт → Прелюдия → Fire
- **Персонализация**: выбор эмодзи для игроков
- **Редактор заданий**: добавление собственных заданий
- **Модерация контента**: одобрение пользовательских заданий
- **Админ-панель**: управление пользователями, статистика, модерация
- **Автоматическое сохранение** состояния игры

## 🔧 Требования к системе

### Минимальные требования VDS:
- **ОС**: Ubuntu 20.04+ или Debian 11+
- **RAM**: 512 MB (рекомендуется 1 GB)
- **CPU**: 1 ядро
- **Диск**: 5 GB свободного места
- **Интернет**: стабильное подключение

### Программное обеспечение:
- Python 3.8+
- pip (менеджер пакетов Python)
- git (для обновлений)

## 🚀 Быстрая установка на VDS

### 1. Подготовка сервера
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Скачайте проект на сервер
git clone <your-repository-url> telegram-bot
cd telegram-bot
```

### 2. Автоматическая установка
```bash
# Сделайте скрипт исполняемым
chmod +x install.sh

# Запустите установку
sudo bash install.sh
```

### 3. Настройка конфигурации
```bash
# Скопируйте пример конфигурации
sudo cp env.example .env

# Отредактируйте конфигурацию
sudo nano .env
```

**Обязательно настройте в .env:**
```env
BOT_TOKEN=your_actual_bot_token_here
OWNER_USERNAME=mpr_xo
ADMIN_USERNAMES=virgo_e
SUPPORT_CONTACT=@Uzumymbec
```

### 4. Запуск бота
```bash
# Запустите бота
telegram-bot start

# Проверьте статус
telegram-bot status

# Просмотрите логи
telegram-bot logs
```

## 🔑 Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

## 👥 Настройка администраторов

### Автоматические администраторы (в коде):
- **Владелец**: `@mpr_xo` (полный доступ)
- **Администратор**: `@virgo_e` (доступ к админ-панели)

### Добавление новых администраторов:
1. Через админ-панель бота (раздел "Управление администраторами")
2. Или измените код в файле `main.py` в методе `ensure_owner_rights()`

## 🛠️ Управление ботом

### Команды управления:
```bash
telegram-bot start    # Запустить бота
telegram-bot stop     # Остановить бота
telegram-bot restart  # Перезапустить бота
telegram-bot status   # Показать статус
telegram-bot logs     # Показать логи
telegram-bot update   # Обновить код из git
```

### Просмотр логов:
```bash
# Логи в реальном времени
telegram-bot logs

# Логи systemd
journalctl -u telegram-bot -f

# Логи приложения
tail -f /opt/telegram-bot/logs/bot.log
```

## 💾 Резервное копирование

### Автоматическое создание бэкапа:
```bash
# Создать резервную копию базы данных
sudo bash backup_db.sh
```

### Восстановление из бэкапа:
```bash
# Восстановить базу данных
sudo bash restore_db.sh
```

### Настройка автоматических бэкапов:
```bash
# Добавить в crontab (ежедневно в 3:00)
sudo crontab -e

# Добавить строку:
0 3 * * * /opt/telegram-bot/backup_db.sh
```

## 🔒 Безопасность

### Настройки безопасности (уже включены в install.sh):
- **Firewall**: настроен для блокировки лишних портов
- **Fail2ban**: защита от брутфорса SSH
- **Пользователь бота**: отдельный пользователь без root прав
- **Виртуальное окружение**: изоляция зависимостей

### Дополнительные рекомендации:
```bash
# Измените SSH порт
sudo nano /etc/ssh/sshd_config
sudo systemctl restart ssh

# Настройте ключи SSH вместо паролей
ssh-keygen -t rsa -b 4096
ssh-copy-id user@server

# Регулярно обновляйте систему
sudo apt update && sudo apt upgrade
```

## 📊 Мониторинг

### Проверка состояния:
```bash
# Статус сервиса
systemctl status telegram-bot

# Использование ресурсов
htop
df -h
free -h

# Сетевая активность
netstat -tulpn | grep python
```

### Логи и диагностика:
```bash
# Основные логи бота
tail -f /opt/telegram-bot/logs/bot.log

# Логи ошибок
tail -f /opt/telegram-bot/logs/errors.log

# Системные логи
journalctl -u telegram-bot --since "1 hour ago"
```

## 🔄 Обновление бота

### Обновление кода:
```bash
# Автоматическое обновление
telegram-bot update

# Или вручную
cd /opt/telegram-bot
sudo -u telegram-bot git pull
sudo -u telegram-bot bash -c "source venv/bin/activate && pip install -r requirements.txt"
telegram-bot restart
```

## 🐛 Решение проблем

### Бот не запускается:
1. Проверьте токен в `.env`
2. Проверьте логи: `telegram-bot logs`
3. Проверьте права доступа к файлам
4. Убедитесь, что порты 80/443 открыты

### Ошибки в логах:
```bash
# Проверьте логи ошибок
tail -f /opt/telegram-bot/logs/errors.log

# Проверьте системные логи
journalctl -u telegram-bot --since "10 minutes ago"
```

### Проблемы с базой данных:
```bash
# Проверьте права доступа
ls -la /opt/telegram-bot/bot.db

# Восстановите из бэкапа
sudo bash restore_db.sh
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь в правильности конфигурации
3. Обратитесь к администратору: **@Uzumymbec**

## 📁 Структура файлов

```
/opt/telegram-bot/
├── main.py              # Основной файл бота
├── database.py          # Работа с базой данных
├── game_data.py         # Данные игр
├── requirements.txt     # Зависимости Python
├── .env                 # Конфигурация (создается при установке)
├── bot.db              # База данных SQLite
├── logs/               # Логи приложения
│   ├── bot.log
│   └── errors.log
├── venv/               # Виртуальное окружение Python
└── install.sh          # Скрипт установки
```

## 🎉 Готово!

После выполнения всех шагов ваш бот будет:
- ✅ Автоматически запускаться при перезагрузке сервера
- ✅ Автоматически перезапускаться при сбоях
- ✅ Вести логи всех операций
- ✅ Создавать резервные копии базы данных
- ✅ Быть защищенным от основных угроз

**Удачного развертывания! 🚀**
