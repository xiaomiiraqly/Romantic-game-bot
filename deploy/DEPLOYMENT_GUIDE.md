# 🚀 ПОШАГОВОЕ РАЗВЕРТЫВАНИЕ TELEGRAM БОТА

## 📋 Краткая инструкция

### Для VDS сервера (Ubuntu/Debian):

1. **Подготовка сервера:**
   ```bash
   # Обновить систему
   sudo apt update && sudo apt upgrade -y
   
   # Скачать проект
   git clone <repository-url> telegram-bot
   cd telegram-bot
   ```

2. **Автоматическая установка:**
   ```bash
   # Быстрое развертывание с настройкой токена
   sudo bash deploy.sh
   
   # ИЛИ пошаговая установка
   sudo bash install.sh
   ```

3. **Настройка токена:**
   ```bash
   # Отредактировать конфигурацию
   sudo nano .env
   # Указать BOT_TOKEN=your_token_here
   ```

4. **Запуск:**
   ```bash
   telegram-bot start
   telegram-bot status
   ```

### Для Windows (разработка):

1. **Запустить установку:**
   ```cmd
   install_windows.bat
   ```

2. **Настроить токен в .env**

3. **Запустить бота:**
   ```cmd
   start_bot.bat
   ```

## 🔑 Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

## 👥 Администраторы бота

- **Владелец**: `@mpr_xo` (полный доступ)
- **Администратор**: `@virgo_e` (админ-панель)
- **Поддержка**: `@Uzumymbec`

## 🛠️ Управление

### Linux команды:
```bash
telegram-bot start    # Запуск
telegram-bot stop     # Остановка  
telegram-bot restart  # Перезапуск
telegram-bot status   # Статус
telegram-bot logs     # Логи
```

### Резервное копирование:
```bash
sudo bash backup_db.sh   # Создать бэкап
sudo bash restore_db.sh  # Восстановить
```

## 📊 Мониторинг

```bash
# Проверка состояния
bash monitor.sh

# Логи в реальном времени
telegram-bot logs

# Системные логи
journalctl -u telegram-bot -f
```

## 🐛 Решение проблем

1. **Бот не запускается:**
   - Проверьте токен в `.env`
   - Проверьте логи: `telegram-bot logs`
   - Проверьте права доступа

2. **Ошибки в логах:**
   ```bash
   tail -f logs/errors.log
   journalctl -u telegram-bot --since "10 minutes ago"
   ```

3. **Проблемы с базой данных:**
   ```bash
   ls -la bot.db
   sudo bash restore_db.sh
   ```

## 📞 Поддержка

При проблемах обращайтесь к: **@Uzumymbec**

---

**Готово! Ваш бот развернут и готов к работе! 🎉**
